{
  "name": "health-check-app",
  "version": "1.0.0",
  "description": "Application with health check endpoint",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.9.7"
  },
  "devDependencies": {
    "jest": "^26.6.0",
    "supertest": "^6.0.1"
  }
}
