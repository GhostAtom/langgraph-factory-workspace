{
  "name": "health-check-example",
  "version": "1.0.0",
  "description": "Example app with a health check endpoint",
  "main": "src/app.js",
  "scripts": {
    "test": "mocha test/*.test.js",
    "start": "node src/app.js"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "mocha": "^9.1.3",
    "supertest": "^6.1.6"
  }
}
