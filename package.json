{
  "name": "status-endpoint",
  "version": "1.0.0",
  "description": "An Express app with a /status endpoint that returns system uptime.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "jest": "^27.0.0",
    "supertest": "^6.1.3"
  }
}
